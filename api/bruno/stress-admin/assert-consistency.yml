openCollection: 1.0.0
type: http-request
info:
  name: Assert Consistency
  description: Validates inventory invariants after manual, concurrency, or stress tests.
request:
  method: GET
  url: "{{baseURL}}/v1/admin/stress/assert-consistency"
  headers: []
  body:
    mode: none
