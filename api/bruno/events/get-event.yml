openCollection: 1.0.0
type: http-request
info:
  name: Get Event
  description: Reads one event by ID.
request:
  method: GET
  url: "{{baseURL}}/v1/events/{{eventID}}"
  headers: []
  body:
    mode: none
