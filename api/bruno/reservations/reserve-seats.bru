meta {
  name: Reserve Seats
  type: http
  seq: 2
}

post {
  url: {{baseURL}}/v1/reservations/seats
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{token}}
}

body:json {
  {
    "event_id": "{{eventID}}",
    "seat_ids": ["{{seatID}}"],
    "idempotency_key": "{{idempotencyKey}}"
  }
}
